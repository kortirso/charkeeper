import { useAppState } from '../../../context/appState';

export const ProfilePage = (props) => {
  const [appState] = useAppState();

  // 453x750
  // 420x690
  return (
    <div>
    </div>
  );
}
