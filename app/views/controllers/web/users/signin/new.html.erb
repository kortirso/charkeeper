<div class="max-w-7xl flex-1 flex flex-col justify-center items-center">
  <%= form_with model: @user, url: signin_index_path, method: :post do |form| %>
    <h2 class="text-2xl mb-4"><%= t('web.users.signin.title') %></h2>
    <div class="form-field mb-2">
      <%= form.label :username, t('web.users.signup.username'), class: 'form-label' %>
      <%= form.text_field :username, class: 'form-value w-full' %>
    </div>
    <div class="form-field mb-2">
      <%= form.label :password, t('web.users.signup.password'), class: 'form-label' %>
      <%= form.password_field :password, class: 'form-value w-full' %>
    </div>
    <div class="mb-2">
      <%= t('web.users.signin.have_not') %>
      <%= link_to t('web.users.signin.signup'), new_signup_path, class: 'underline text-blue-600' %>
    </div>
    <%= form.submit t('web.users.signin.create'), class: 'btn btn-primary' %>
  <% end %>
</div>
