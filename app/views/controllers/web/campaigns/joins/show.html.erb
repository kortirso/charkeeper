<%=
  js_component 'CharKeeperApp',
               access_token: @access_token,
               locale: locale: current_user.locale == 'es' ? 'en' : current_user.locale,
               username: current_user.username,
               admin: current_user.admin?,
               color_schema: current_user.color_schema,
               join_campaign_id: params[:campaign_id],
               component_id: 'root',
               host: root_host
%>
